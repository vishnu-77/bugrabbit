# Backlog archive — bug-fixing system

Settled `FIX-NNN` rows (`DONE` / `SKIPPED`) relocated from `docs/backlog.md` by `/archive-task`, so
the live backlog stays lean. Rows are moved **verbatim** with an archive date. Findings stay in
`docs/findings.md` (append-only); the durable per-issue record stays in `docs/issue-log.md`.

| FIX-NNN | repository | issue # | title | severity | final status | branch | created (UTC) | updated (UTC) | archived (UTC) |
|---------|------------|---------|-------|----------|--------------|--------|---------------|---------------|----------------|
| — | — | — | (none yet) | — | — | — | — | — | — |

## Archived task bodies

<!-- Moved verbatim from backlog.md by /archive-task, newest first. -->
